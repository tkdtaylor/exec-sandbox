# Test Coverage Tracker

**Project:** exec-sandbox

## Rules

- Test specs are written **before** implementation begins — no exceptions
- A task is **not** "complete" because the feat commit landed and tests passed. See the verification ladder below.
- Each row maps a task ID to its spec file, current test status, and the verification level achieved

## Coverage

| Task ID | Feature | Spec file | Tests written | Status | Verified by |
|---------|---------|-----------|---------------|--------|-------------|
| 001 | gVisor (runsc) Tier-2 backend behind the `tier` seam | [`001-gvisor-tier2-backend-test-spec.md`](001-gvisor-tier2-backend-test-spec.md) | TC-001..007 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 ./...` 8/8 pass; `TestGvisorRunReachesAllowlistedHostAndBlocksOthers` ran (not skipped) under runsc — `allow=200`, `block=403`, direct net `FAILED-no-network`, `tier=gvisor`. Verifier confirmed no-network OCI netns, proxy-only egress, and `runsc --host-uds=open` connect-only confinement against authoritative flag semantics. |
| 002 | Enforce `profile.limits` (cpu/mem/pids/disk/timeout) on bwrap + gVisor | [`002-enforce-profile-limits-test-spec.md`](002-enforce-profile-limits-test-spec.md) | TC-001..011 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 ./...` 17 PASS / 0 SKIP on a host with bwrap+runsc+taskset+prlimit — every cap proven behaviorally: memory 256MB alloc OOM'd under 64MB (RLIMIT_AS); fork bomb hit "Cannot fork" under pids=20 (RLIMIT_NPROC); 4MB write to a 1MB `/tmp` hit ENOSPC; `sleep 30` killed in ≈1s with `status="timeout"`; in-box `nproc==1` under cpu_count=1; `diskQuotaSupported=false` ⇒ run still succeeds with a stderr WARNING + `degraded:[disk_mb]`; gVisor enforced mem/pids/disk via OCI `process.rlimits`+tmpfs `size=`. `run_test.go`/`gvisor_test.go` unmodified & green; `gofmt -l .` clean. No "not yet enforced" caveat remains for limits in docs/spec or CONTRACT. |
| 003 | Writable working-directory mount (`run.workdir` → `/work` rw, cwd=/work) on bwrap + gVisor | [`003-writable-workdir-mount-test-spec.md`](003-writable-workdir-mount-test-spec.md) | TC-001..010 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 ./...` 26 PASS / 0 SKIP / 0 FAIL on a bwrap+runsc host — workdir behaviorally proven on BOTH tiers: host-seeded `/work/seed.txt` read back, payload's `/work/out.txt` write persisted to the host dir, `pwd==/work`; `TestWorkdirEndToEnd_Gvisor` ran (not skipped). Writability proven negatively: `/work` is `--bind`/non-`ro` (not `--ro-bind`), a `/usr` write hit read-only, `--unshare-all` kept, no `--share-net`, OCI netns path-less. Bad path → `{error}` before proxy/vault (no side effect); absent workdir → no `/work`, prior behavior intact. `run_test.go` unmodified vs ab03804; `gofmt -l .` clean. |
| 004 | `FileRead{paths}` read-only host mounts + payload PATH/env provisioning (bwrap + gVisor) | [`004-toolchain-mount-and-path-test-spec.md`](004-toolchain-mount-and-path-test-spec.md) | TC-001..011 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox` 37 PASS / 0 SKIP / 0 FAIL on a bwrap+runsc host. FileRead behaviorally proven on BOTH tiers: a host marker tool mounted read-only is read+executed and resolves via `command -v` on `run.env["PATH"]` (`TestFileReadOnPathResolves_Bwrap`, `TestFileReadEndToEnd_Gvisor` ran — not skipped). Read-only proven negatively: a write under the FileRead mount fails and the host `evil.txt` is never created while `/work` write persists (`TestFileReadMountIsReadOnly_Bwrap`); argv uses `--ro-bind` not `--bind`, OCI options contain `"ro"`, `--unshare-all` kept, no `--share-net`, netns path-less. Bad path (relative/nonexistent) → `{error}` before proxy/vault. Empty FileRead/env ⇒ base argv/spec byte-for-byte unchanged. ADR 005 written; `gofmt -l .` clean; `run_test.go`/`gvisor_test.go` unmodified. spec-verifier APPROVE: all 11 TCs ✓ with real assertions, base argv/OCI spec byte-for-byte unchanged, netns invariant intact, read-only proven via host ground-truth. |
| 006 | Per-run stdout/stderr output caps (`profile.limits.max_output_bytes`) — completes per-run resource bounding, tier-independent | [`006-output-caps-test-spec.md`](006-output-caps-test-spec.md) | TC-006-01..07 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox` 0 FAIL, all 7 task-006 TCs PASS / 0 SKIP on a bwrap+runsc host. `TestOutputCapTruncates_Bwrap` and `_Gvisor` both RAN — a real 1 MiB payload truncated to `len(stdout)==1024`, exit 0, `output_truncated:["stdout"]`, identical under bubblewrap and gVisor. Cap proven above the tier seam: `gvisor.go`/`proxy.go`/`main.go` zero-diff, bwrap argv DeepEqual-unchanged (TC-006-05), `--unshare-all` kept, no `--share-net`/new mount, netns path-less. capWriter unit-tested exact-cap/cap+1/chunked; uncapped run → full 50000 bytes, `output_truncated:[]`. + L3 fitness F-008. ADR 007 written; spec (data-model/configuration/behaviors B-009/fitness F-008) + CONTRACT updated in place. |
| 007 | Per-HTTP-verb allowlist enforcement in the egress proxy (allowed verb forwards; disallowed → 403, no upstream) | [`007-proxy-verb-allowlist-test-spec.md`](007-proxy-verb-allowlist-test-spec.md) | TC-007-01..08 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 -v ./...` → 53 PASS / 0 FAIL / 0 SKIP on a bwrap host. Verb integration TCs (TC-007-05/06/08-integ) RAN under bwrap — allowed verb GET 200 + 1 origin hit, disallowed POST 403 + **0 origin hits** (no outbound connection, no credential injection on the block path), unconstrained host forwards all verbs (backward compat). Unit TCs prove `blocked-by-method` vs `blocked-by-allowlist` distinct 403 bodies, host check precedes verb check, case-insensitive match (get/Get/GET pass, delete rejected), absent + empty `methods:[]` both = unconstrained. Invariant intact: no new route/--share-net/socket; verb check narrows only. + L3 fitness F-009. ADR 008 written; spec (data-model/configuration/behaviors B-002/fitness F-009) + CONTRACT + diagrams updated in place. |
| 008 | Snapshot/restore for clean-slate sandbox reuse — leak-proof reset behind the unchanged one-shot `run()` | [`008-snapshot-restore-test-spec.md`](008-snapshot-restore-test-spec.md) | TC-008-01..07 written + passing | ✅ verified | L6 (spec-verifier APPROVE): `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox` 60 PASS / 0 SKIP / 0 FAIL on a bwrap host. Both L6 leak tests RAN un-skipped: `TestSecondRunCannotSeeFirstRunFiles_Bwrap` — run 1 writes `/work/leak.txt`, after host-side restore run 2 prints `CLEAN` (not `LEAKED`); `TestOneShotRunUnchanged_Bwrap` — one-shot run returns 200 via proxy + full sandbox_status schema (byte-for-byte unchanged). Load-bearing assertions pass: **restored == fresh** diff (surface + payload.sh + creds, `reflect.DeepEqual`) and credential map empty after restore (`proxy.Wipe()` subsumed). Restored argv keeps `--unshare-all`, no `--share-net`, same fresh `/proxy.sock`; backend.Argv inputs byte-for-byte identical pre/post refactor. Existing `run_test.go`/`gvisor_test.go` unmodified + green. + L3 fitness F-010. ADR 009 written (four open-question dispositions: host-side-only baseline, no warm-pool, no reuse trigger, host-side tier-common reach); spec (behaviors B-012 / fitness F-010) + diagrams updated in place; no future-tense drift. |
| 009 | Wire the fitness functions — `fitness-<id>` Make targets per block rule + `fitness:` umbrella (9 block rules, F-003 excluded) + 3 new checks (F-001 bwrap-side, F-002 cred-leak, F-004 prefix-bound) | [`009-wire-fitness-functions-test-spec.md`](009-wire-fitness-functions-test-spec.md) | TC-009-01..12 written + passing | ✅ verified | L5/L3 (spec-verifier APPROVE, all TC-009-01..12 ✓): `make fitness` → `All fitness checks passed.` runs 9 block rules live under bwrap — F-006..F-010 exercise the live runtime path (L5 for the wrapped invariants); umbrella excludes `fitness-no-deps` (proven via `make -n fitness`). 3 NEW checks L2 with proven-biting negatives: F-001 rejects argv missing `--unshare-all` / containing `--share-net` + gVisor netns-with-path; F-002 catches sentinel in argv/`--setenv`/stdout; F-004 rejects full handle + `credential`/`value` keys. `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox`; `gofmt -l .` clean. No new runtime CLI surface — the fitness targets ARE the harness. `fitness_test.go` + `Makefile` added; `docs/spec/fitness-functions.md` flipped F-001/F-002/F-004 to active, removed "not wired" caveats. |
| 010 | Terminal audit event on early proxy-start failure — resolve B-007 TODO (a started run emits a terminal event even on early failure) | [`010-early-proxy-failure-audit-test-spec.md`](010-early-proxy-failure-audit-test-spec.md) | TC-010-01..06 written + passing | ✅ verified | L5 (spec-verifier APPROVE, all TC-010-01..06 ✓): recording stub audit socket — forced proxy-start failure records `spawn` then `exit`/deny/`status:proxy_start_failed` event, `Run()` returns `{error:"proxy start failed:…"}`, no payload run; success path records exactly `spawn→exit`/allow, no spurious extra event; empty audit_socket → no-op, error returned unchanged. `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox`. ADR 013 written (event shape settled against audit IPC contract); B-007 TODO removed; `data-model.md` audit-event section updated with two `exit` context shapes. |
| 011 | Signed `sandbox_identity` attestation — replace random bytes with a verifiable signature (ephemeral ed25519 self-attestation, stdlib-only) | [`011-attestation-signatures-test-spec.md`](011-attestation-signatures-test-spec.md) | TC-011-01..06 written + passing | ✅ verified | L5 (spec-verifier APPROVE, all TC-011-01..06 ✓): recording stub vault socket — vault.inject receives a well-formed `sandbox_identity:{sandbox_id, nonce, ts, attestation_pubkey, attestation}` that re-verifies after JSON round-trip (handle/mode unchanged). L2 crypto unit-proven: mint→verify accepts; three tamper variants (mutated sandbox_id / signature / nonce) all FAIL verification (non-vacuous, `t.Fatalf` asserted); mint+verify share one canonical preimage helper. Private key never escapes `mintAttestation` — only 32-byte pubkey + 64-byte signature externally visible (structural leak-scan over identity blob). ADR 014 (ephemeral ed25519 self-attestation; vault trust anchor = uid-restricted socket + single-use handle binding, not host key). Stdlib-only — `make fitness` green, F-003 intact. `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox`; `gofmt -l .` clean. |
| 012 | Env-mode credential injection + wipe clock — deliver the credential to the sandbox env (env-mode only) and wipe it per a defined clock; proxy-mode "never enters the sandbox" unchanged | [`012-env-mode-injection-wipe-clock-test-spec.md`](012-env-mode-injection-wipe-clock-test-spec.md) | TC-012-01..07 written + passing | ✅ verified | L5 (spec-verifier APPROVE, all TC-012-01..07 ✓; off-argv / `--clearenv` / proxy-mode-F-002 invariants scrutinized + held): `go test -count=1 ./...` → `ok github.com/tkdtaylor/exec-sandbox` (all PASS) on a bwrap host. L5/L6 runtime evidence (un-skipped): `TestEnvModeDeliversCredentialToSandbox` — payload `printf '%s' "$API_TOKEN"` reads the env-mode credential from its env (stdout == sentinel); `TestEnvModeValueAbsentFromArgvResultAudit` — sentinel absent from the captured spawn argv (`--args FD` delivery, no `/proc/cmdline` leak), from stdout/`sandbox_status`, and from every audit event; `TestEnvModeMixedRunKeepsProxyOut` — mixed env+proxy run delivers env IN while the proxy-mode credential stays OUT of sandbox stdout/argv/audit and is injected only into the outbound request header (F-002 intact); `TestEnvModeInjectFailureSkipsHandle` — failed inject emits `inject_failed`, delivers no var (`UNSET`), run continues, no `secrets_injected` entry; `TestEnvCredentialsWipeClearsHostCopy` — host-side holder cleared by Wipe (wipe clock). + L3 fitness: F-002 cred-not-in-sandbox still green for proxy-mode; `make fitness` → `All fitness checks passed.` ADR 015 written (wipe-clock = post-spawn/teardown; off-argv delivery via bwrap `--args FD` + gVisor OCI `process.env`; vault env-mode fields `credential`/`var_name`/`wiped_at`; `cmd.Env` rejected because `--clearenv` defeats it). Spec updated in place: behaviors B-003 (env vs proxy distinction + wipe clock), data-model (env response delivered + credential data-invariant with env exception), configuration (`injection_mode` "env" row — "recorded but not loaded" removed). |
| 013 | Firecracker Tier-3 dispatch + config-generator skeleton — wire `firecrackerBackend` into `backendFor`; generate the microVM config (machine-config/boot-source/root-drive/vsock) as a PURE function of on-host paths; NO `network-interface` key (no-NIC by construction); no VMM launch (ADR 010 D1/D2/D3) | [`013-firecracker-tier-dispatch-config-skeleton-test-spec.md`](013-firecracker-tier-dispatch-config-skeleton-test-spec.md) | TC-013-01..08 specced; not yet written | ❌ planned (not started) | — (planning only; no code written). **READY — the thin first slice, no open-question dependency.** Target: L4 (per ADR-010 table; load-bearing L2 unit — config builds without `/dev/kvm`, carries no NIC, deterministic; `backendFor` dispatches; + L3 no-NIC fitness via task 018). No ADR (ADR 010 accepted). Dep root: 013 → 014 → 015 → {016,017} → 018. |
| 014 | No-NIC + vsock-bridge egress enforcement — wire vsock to the live `EgressProxy`, ship the dumb guest-side vsock→/proxy.sock shim, prove no NIC + credential never reaches the guest (ADR 010 D2; resolves Q4) | [`014-no-nic-vsock-bridge-egress-test-spec.md`](014-no-nic-vsock-bridge-egress-test-spec.md) | TC-014-01..11 specced; not yet written | ❌ planned (not started) | — (planning only). **READY after 013; the egress crux, highest assurance.** Target: **L6** (per ADR-010 table — allowlisted host 200 / non-allowlisted 403 / direct-net fails over `/proxy.sock`-in-guest; credential absent from guest env/args/stdout). Q4 (shim location/lifecycle) is an in-task decision recorded in `behaviors.md` + ADR-010 note. Dep: 013 → **014**. |
| 015 | Guest boot — kernel image + rootfs + **direct firecracker launch (no jailer)** via REST-over-Unix-socket; payload runs through the unchanged host-side capture path (ADR 010 D1/D3 + Amendment 1) | [`015-guest-boot-kernel-rootfs-jailer-test-spec.md`](015-guest-boot-kernel-rootfs-jailer-test-spec.md) | TC-015-01..10 specced; not yet written | 📋 planned (ready) | — (planning only). **Q1+Q3 resolved — ADR-010 Amendment 1 (2026-06-20):** Q1 = build-from-source pinned `vmlinux` + Alpine RO rootfs, sha256-verified fail-fast; Q3 = **no jailer**, run `firecracker` directly under `bwrap --unshare-all` + `limits.go` (host user in `kvm` group; no root). Target: L5 (per ADR-010 table — guest boots, payload prints + exits, timeout=137, missing prereq → exit 127 no fall-back; constraints ≥ jailer per TC-015-05). Dep: 013 → 014 → **015**. |
| 016 | profile.limits → machine-config mapping — cpu_count→vcpu_count, memory_mb→mem_size_mib, disk_mb→drive size, pids→in-guest RLIMIT_NPROC; timeout_sec/max_output_bytes stay host-side above the seam (ADR 010 D4) | [`016-profile-limits-machine-config-mapping-test-spec.md`](016-profile-limits-machine-config-mapping-test-spec.md) | TC-016-01..11 specced; not yet written | ❌ planned (not started) | — (planning only). **READY once 013 + 015 land; no open-question block of its own** (Q1/Q3 inherited via 015). Target: L5 (per ADR-010 table — over-memory payload OOMs, fork bomb hits NPROC, `nproc==1`; config byte-for-byte independent of timeout/output). No new ADR. Dep: 013 → 014 → 015 → **{016,017}**. |
| 017 | /work + FileRead mount semantics in the microVM — present writable `/work` (single writable surface) + read-only FileRead paths, preserving ADR 004/005 (ADR 010 D3; resolves Q2) | [`017-work-fileread-mount-semantics-test-spec.md`](017-work-fileread-mount-semantics-test-spec.md) | TC-017-01..09 specced; not yet written | ⚠️ planned, BLOCKED on Q2 | — (planning only). **BLOCKED on Q2 (mount mechanism: block device vs virtio-fs vs copy-in/out) — a smaller in-task decision (no ADR amendment required), recorded in `behaviors.md` + ADR-010 note at the top of the task; ready to start once 013 + 015 land and Q2 is decided.** Target: L5 (per ADR-010 table — host-seeded `/work` read in-guest, guest write persists, FileRead write fails + host file unchanged, only `/work` writable). Dep: 013 → 014 → 015 → **{016,017}**. |
| 018 | Teardown + spec/diagram sync + microVM fitness functions — terminate the microVM + reclaim cgroup/namespace/bundle at teardown; rewrite SPEC.md Non-goals + diagrams.md; add `fitness-no-nic` (microVM F-001) + `fitness-cred-not-in-guest` (microVM F-002) + **`fitness-constraints-ge-jailer`** (ADR-010 Amendment 1 A1.Q3), coordinated with task 009; VMM-native snapshot OUT of scope (ADR 010 D5/D6) | [`018-teardown-spec-diagram-sync-test-spec.md`](018-teardown-spec-diagram-sync-test-spec.md) | TC-018-01..15 specced; not yet written | ❌ planned (not started) | — (planning only). **READY once 013–017 land; coordinates with task 009's `make fitness` umbrella.** Target: L5 (per ADR-010 table — no firecracker process/cgroup/bundle survives any exit path; `make fitness` green incl. the three new firecracker rules — no-NIC, cred-not-in-guest, **constraints-≥-jailer** asserting non-host uid + all-namespaces-unshared + cgroup limits + chroot, the no-jailer test obligation from Amendment 1 A1.Q3). `snapshot.go` unchanged. No new ADR. Dep terminal: 013 → 014 → 015 → {016,017} → **018**. |
| 019 | Tier-1 seccomp-BPF default-deny syscall profile — `bwrapArgv` passes `--seccomp <fd>` (a build-time-generated, sha256-pinned cBPF blob from plain-text `seccomp/tier1-policy.json`); default-deny + allowlist modeled on Docker's profile; deny `keyctl`/`ptrace`/`userfaultfd`/`bpf`/`perf_event_open`/`mount`-family/`kexec`/module-family; stdlib loader fails fast on sha256 mismatch (no unfiltered fall-back); NO Go third-party runtime dep (libseccomp is build-time only). **Tier-1 ONLY** — gVisor's sentry + Firecracker's self-installed filters make it redundant on Tier-2/3 | [`019-tier1-seccomp-profile-test-spec.md`](019-tier1-seccomp-profile-test-spec.md) | TC-019-01..11 specced; not yet written | 📋 planned (ready) | — (planning only; no code written). **READY — design settled by a primary-source research pass; no open-question block.** Target: **L6** (real bwrap run — blocked `keyctl` returns EPERM under Tier-1, common-case payload still exits normally; + L2 loader/argv units incl. sha256 fail-fast negative; + L3 `fitness-tier1-seccomp` positive/negative). **ADR (next available number) required during implementation** — Tier-1 scope + deny set + build-time-cBPF/sha256-pin design. **Dependency-coordination:** the new fitness rule registers into **task 009's `fitness:` umbrella** once 009 lands (like task 018's microVM rules); otherwise independent of the 013–018 firecracker epic. |

## Status key

| Symbol | Meaning |
|--------|---------|
| ✅ | **Verified** — validation harness exercised the live runtime path, or operator observed the targeted behaviour |
| 🟡 | **Code merged** — feat-commit landed, unit tests + fitness + CI green, but runtime/live behaviour not yet observed |
| ⏳ | In progress |
| ❌ | Not started |
| ⚠️ | Blocked |

## Verification ladder

A task earns 🟡 at levels 1–4 and ✅ only at level 5 or 6. The `Verified by` column records which level the row reached.

| Level | Evidence | Status this earns |
|-------|----------|-------------------|
| 1 | Code merged | 🟡 |
| 2 | Unit tests pass (paste verbatim final line of `make check`) | 🟡 |
| 3 | `make fitness` passes (verbatim closing line) | 🟡 |
| 4 | CI passes (`gh run watch <id> --exit-status` → success) | 🟡 |
| 5 | **Validation harness** exercises the live runtime path end-to-end — paste the command and the final assertion line | ✅ |
| 6 | **Operator-observed** — operator (or executor via `cargo run` / `npm start` / etc.) saw the targeted behaviour in stdout / logs / UI | ✅ |

If the task targets runtime-observable behaviour (logging, CLI args, TUI, server endpoints, file outputs, side effects), level 5 or 6 is **required** before flipping to ✅. If the task only adds an internal helper covered by unit tests, level 2 may be sufficient — but in that case the row's `Verified by` should explicitly say "unit-test-only; no runtime surface" so future readers don't mistake silence for verification.

## Rule

**The task-executor commits at 🟡 by default.** Only the main session (after spec-verifier APPROVE + the appropriate level-5/6 evidence) updates the row to ✅, in a separate commit titled `verify: confirm task NNN — <level-5/6 evidence>`. This keeps the verification step visible in git history and prevents "merged ≠ done" drift.
